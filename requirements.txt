# --- core scientific stack (CPU only; no GPU, no paid API) ---
numpy>=1.26
pillow>=10.2
pyyaml>=6.0
tqdm>=4.66

# --- supervised models ---
torch>=2.2
torchvision>=0.17
scikit-learn>=1.4
joblib>=1.3

# --- evaluation / reporting ---
pandas>=2.2
matplotlib>=3.8

# --- serving (free-tier friendly) ---
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# --- analytical face/consistency module (optional but recommended) ---
# 4.x is deliberate: the 5.0 wheels no longer ship cv2/data/haarcascades, and the face
# module needs those XML cascade files to detect faces at all.
opencv-python-headless>=4.10,<5

# --- tests (optional; `python -m pytest -q`) ---
# pytest>=8.0
# httpx>=0.27          # used by fastapi.testclient
